//: [Previous](@previous)

import Foundation

var escudos: Int = 5
var conteoBalasDisparadas: Int = 0
var armaSobrecalentada: Bool = false

func disparar(){
    while(escudos && !armaSobrecalentada){
        conteoBalasDisparadas++
        if(conteoBalasDisparadas==200){
            
        }
    }
}
